---
description: 每个子任务完成后，告知用户可体验的命令和方式
alwaysApply: true
---

# 可体验指引规则

## 核心要求

每个子任务完成后，**必须**在回复末尾附上一个"立即体验"区块：

```
**立即体验**
# 命令描述
cd /项目路径 && source .venv/bin/activate
python3 命令或脚本
```

## 体验区块格式

- 给出**可直接复制执行**的完整命令（含 cd 和激活 venv）
- 说明预期输出是什么，让用户知道"看到什么就算成功"
- 如有仪表盘页面，也附上 streamlit 启动命令

## 适用范围

- 每完成一个子任务（X.Y）时
- 修复 bug 后
- 安装新依赖后
