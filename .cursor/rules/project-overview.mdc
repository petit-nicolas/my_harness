---
description: Harness 项目背景、计划概览与全局约定，所有对话必须遵循
alwaysApply: true
---

# Harness 项目概览

## 项目定位

本项目是一个**教学框架**，目标是从零复现类似 Claude Code 的极简 Coding Agent。
不是追求生产级完备，而是让学习者理解 Agent 架构的每个核心组件。

## 技术栈

- 语言：Python 3.11+
- 大模型：阿里千问（qwen-plus），通过 OpenAI SDK 兼容模式对接
- API 密钥：`.ENV` 文件中的 `Ali_API_KEY`
- 终端 UI：`rich` 库
- 配套前端：Streamlit 轻量仪表盘

## 计划结构（5 阶段 12 步骤）

| 阶段 | 步骤 | 核心目标 |
|------|------|----------|
| 准备 | Step 0 | 项目规则、骨架、仪表盘框架 |
| 骨架 | Step 1-3 | API 对接 → 提示词 → Agent 循环 |
| 能力 | Step 4-6 | 精准编辑 → 搜索 → 安全机制 |
| 体验 | Step 7-9 | 流式输出 → 重试压缩 → 会话持久化 |
| 进阶 | Step 10-11 | 记忆系统 → Hooks 扩展 |

详细计划见 `PLAN.md`，实时进度见 `PROGRESS.md`。

## 关键文件

| 文件 | 用途 |
|------|------|
| `PLAN.md` | 完整实施计划 |
| `PROGRESS.md` | 进度追踪 + 子任务清单 + 变更日志 |
| `res/*.pdf` | 12 份 Claude Code 架构研究文档 |
| `.ENV` | API 密钥（不得提交到 git） |
| `dashboard/` | Streamlit 教学仪表盘 |

## 全局约定

- 永远用中文回答
- 每次对话开始时，先读取 `PROGRESS.md` 确认当前进度
- 不要一次性完成整个 Step，按子任务逐步推进
- 每个子任务完成后更新 `PROGRESS.md`
- 代码注释用中文，变量和函数命名用英文 snake_case
